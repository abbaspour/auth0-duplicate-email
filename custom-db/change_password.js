function changePassword(email, newPassword, callback) {
    console.log('FE changePassword for email: ' + email);
    // use management API to change password in backend
    return callback(null, true);
}

